<div class="row">
  <div class="col-xl-12 text-center">
    <h3><%=@user.username%>'s Page</h3>
    <br>
    <%@user.items.each do |item|
        case item.share
        when *['edit','view']%>
          <h5><a href="/items/<%=item.id%>"><%=item.name%></a></h5><p><%=item.description%></p>
      <%else%>
          <%if current_user.items.include?(item)%>
            <h5><a href="/items/<%=item.id%>"><%=item.name%></a></h5><p><%=item.description%></p>
          <%end%>
      <%end%>
       <hr>
    <%end%>
  </div>
</div>
